# ChatMaker v0.1.0-rc1

这是面向教师培训和入门黑客松试运行的首个发布候选。

## 已验证

- ChatMaker、ChatDuino、ChatWeb 三个 Skill 通过格式校验。
- Nano 运行层可发现 Mind+ 1.x/2.x、过滤蓝牙串口、执行真实编译并在唯一有线端口时安全烧录。
- 六个 Nano 示例通过 Mind+ 2.x 真实编译。
- 首批 8 种元器件和 7 个项目配方通过资料库校验。
- ChatWeb 可推荐 3 套方向、生成单文件课堂页和模拟硬件页，并仅在 localhost 预览。
- 课堂页和模拟硬件页通过真实浏览器点击、手机视口、触控尺寸与零控制台错误检查。
- Codex 与 WorkBuddy 支持可逆安装；干净环境和当前电脑上的 Blink 编译烟测通过。

## 仍需现场验证

- 当前没有连接有线 Nano，因此没有本次发布候选的烧录、串口运行、断电重启或实物灯光证据。
- Uno、ESP32 和真实网页硬件通信尚未达到发布验证状态。
- 完全不依赖 Mind+ 的工具链不属于本发布候选。

这些限制是明确的证据边界，不会由编译成功或模拟页面替代。
