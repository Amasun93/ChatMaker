schema_version: "1.0"
kind: component
id: idmd-0021-starcore-oled-1-3
hardware_id: IDMD-0021
name: IDMD-0021 星核板 1.3 寸 OLED
aliases: [星核1.3寸OLED, 自研OLED显示模块, IDMD-0021显示屏]
category: display
interface: i2c-ssd1306-128x64
supply_voltage:
  minimum: 3.3
  maximum: 5.0
logic_boundary: Insert the complete module into a matching-voltage I2C bank; P19/P20 are shared, and a 5 V pull-up must not reach the ESP32 bus.
source_ids: [starcore-module-catalog-snapshot, starcore-classroom-module-cards-snapshot, starcore-code-patterns-snapshot, starcore-board-pin-map-snapshot]
sources:
  - title: ChatMaker governed Starcore source manifest
    url: https://github.com/Amasun93/ChatMaker/tree/main/knowledge_sources
verification:
  source_reviewed:
    status: verified
    checked_at: "2026-08-17"
    evidence: Governed owned sources identify IDMD-0021 as an SSD1306 128x64 I2C module using address 0x3C or 0x3D.
  code_compiled:
    status: verified
    checked_at: "2026-08-18"
    evidence: starcore-idmd-0021-oled-message-compile-2026-08-18
  firmware_uploaded: &unverified
    status: unverified
    checked_at: null
    evidence: null
  physical_effect_verified: *unverified
historical_lead:
  status: legacy_reported
  evidence: Legacy material reports OLED use; current compilation is tracked separately, while display output on hardware remains unchecked.
pins:
  - id: VCC
    role: power
  - id: GND
    role: ground
  - id: SCL
    role: i2c-clock
  - id: SDA
    role: i2c-data
supported_boards: [idmc-0001-starcore-v4-2-2]
constraints:
  - Use the MPython.h global display object; do not add U8g2 or a second SSD1306 driver.
  - Scan or confirm 0x3C versus 0x3D rather than guessing when the screen stays blank.
  - The source-reviewed card does not authorize drawPixel, clear, or clearDisplay APIs.
identification:
  - Confirm IDMD-0021, 1.3-inch 128x64 OLED, SSD1306, and I2C pins before using this owned card.
libraries:
  - name: Mind+ MPython display API
    headers: [MPython.h]
    status: compile_verified
    notes: MPython.h supplied the global display object during the current Mind+ 1.8 compilation; display output remains unverified.
mindplus:
  extension: esp32
  headers: [MPython.h]
  api: display.begin(0x3C); display.setCursorLine(); display.printLine(); display.fillInLine(); display.point()
example_files:
  - examples/chatduino/starcore/starcore-idmd-0021-oled-message/starcore-idmd-0021-oled-message.ino
common_failures:
  - Using an SH1106, SPI, or generic 0.96-inch module recipe can select the wrong driver, pinout, or dimensions.
  - A wrong address or mismatched-voltage I2C bank leaves the display blank or risks the ESP32 bus.
board_notes:
  idmc-0001-starcore-v4-2-2: Use a matching-voltage I2C connector; P19 is SCL and P20 is SDA on the shared bus.
